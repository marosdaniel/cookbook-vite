import { Container, Typography } from '@mui/material';

const RecipeFormDisplayer = () => {
  return (
    <Container maxWidth="xl">
      <Typography variant="h3">Recipe Form Displayer</Typography>
    </Container>
  );
};

export default RecipeFormDisplayer;
